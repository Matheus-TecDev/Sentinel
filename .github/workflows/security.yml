name: Security

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    name: Reusable security scan
    uses: Matheus-TecDev/platform-workflows/.github/workflows/security-scan.yml@4741c2c5a01a194a0caa3f066d9433e278173660
