name: Security

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    name: Reusable security scan
    uses: Matheus-TecDev/platform-workflows/.github/workflows/security-scan.yml@v1
